App\Entity\Borrow:
  borrow{1..500}:
    startDate: '<startDate()>'
    endDate: '<endDate($startDate)>'
    car: '@car*'
    driver: '@employed*'
    userEmployed: ['@employed*']
    borrowMeet: '@borrowMeet*'
