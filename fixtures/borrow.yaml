App\Entity\Borrow:
  borrow{1..150}:
    startDate: '<startDate()>'
    endDate: '<endDate($startDate)>'
    car: '@car*'
    driver: '@employed*'
    userEmployed: ['@employed*']
    borrowMeet: '@borrowMeet*'
